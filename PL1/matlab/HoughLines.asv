function [rhos, thetas] = HoughLines(H, nLines)
%Your implemention here
    % firstly for the non maximum supression
    H_copy = H;
    for i = 2:size(H,1)-1
        for j = 2:size(H,2)-1
            interest_region = H(i-1:i+1, j-1:j+1);
            
        end
    end
end
        